
function multipleDe7() 
    for (var i = 7;  i <= 199; i ++){
        if (i % 7 == 0) {
            console.log(i + "is multiple de 7") ;
            
        }
    }

    //nouvelle fonction


    function moyenne(a, b) {
        let add = a + b;
        midpoint = add/2;
        console.log(" the midpoint is = ", midpoint );
    
    } 

    //nouvelle fonction


    function max(a, b) {
     
    
        console.log(" the max number is = ", Math.max ( a, b) );
    
    }

    //nouvelle fonction

    let array = [2,8,20,11];
    for ( i = 0, len = array.length; i <= len; i++){
    console.log(arr[i])};
// function invocation

multipleDe7();
max();
moyenne();






   